spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smarttelecom_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Kanny@2317
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # use 'validate' or 'update' or 'create' based on your needs
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081  # example port for plan-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # if you're using Eureka for service discovery
    register-with-eureka: true
    fetch-registry: true

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
